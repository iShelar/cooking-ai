[build]
  publish = "dist"
  functions = "netlify/functions"

# Share links are now handled by the Python backend (server/share_preview.py).
# In production, proxy /share/* to the backend server.

# SPA: serve index.html for all other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
